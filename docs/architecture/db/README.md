# Database Plans

| File | What it covers |
|---|---|
| [schema_consolidation_plan.sql](schema_consolidation_plan.sql) | SQL plan for consolidating duplicate and legacy tables into the canonical schema |

Applied migrations live in `supabase/migrations/` at the repository root, not here.
