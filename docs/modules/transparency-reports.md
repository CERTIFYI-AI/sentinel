# Transparency Reports

**Route:** `/transparency-reports` · **Backing:** `transparency_reports` (model_id, version, published_at, url)

Reads the same table the mesh's NarrativeEngine agent writes (mesh-generated rows are labelled with their source event). Publish stamps `published_at`; download is a real artifact of the stored content. Reports link to their model.
