# Compliance Autopilot

**Route:** `/autopilot` · **Backing:** `agent_registry` + `agent_executions` (the governance mesh)

An honest compliance-lens view over the REAL agent fleet and its execution ledger — not a separate agent id-space. Cross-links to `/governance-mesh` (full fleet) and `/automation-studio` (rules). Empty execution history renders an honest empty state.
