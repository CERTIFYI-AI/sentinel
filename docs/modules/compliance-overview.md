# Compliance Overview

**Route:** `/compliance` · **Backing:** `frameworks`, `controls`, `compliance_scores`

Derived posture dashboard: framework scores from real `frameworks` rows, control coverage from real `controls`, and recent recalculations from `compliance_scores` — the table the mesh's ComplianceImpact agent writes, so this screen reflects live governance telemetry. No literal scores; "—" where nothing is measured yet.
