// SPDX-License-Identifier: Apache-2.0
// Copyright (c) 2026 CERTIFYI-AI.
//
// RETIRED — superseded by biasAuditRecordsService.ts, which consolidates the
// Bias Audits page and Model Detail's Bias History onto the single
// `bias_audits` table. Kept only so stale imports fail loudly at build time
// rather than silently resolving; delete this file once nothing references it.

export {}
