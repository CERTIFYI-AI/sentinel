# Supabase — production project: vhparvughsygyknblkzt
# Get the anon (publishable) key from:
#   Supabase > Project Settings > API > Project API keys > anon public
VITE_SUPABASE_URL=https://vhparvughsygyknblkzt.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# Run with no backend (all queries return empty results) — useful for UI-only work:
# VITE_DEMO_MODE=true

# Optional: only needed if running edge functions locally (NEVER ship to the frontend bundle)
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Optional: Cloudflare
# CF_ACCOUNT_ID=your-account-id
# CF_API_TOKEN=your-api-token
