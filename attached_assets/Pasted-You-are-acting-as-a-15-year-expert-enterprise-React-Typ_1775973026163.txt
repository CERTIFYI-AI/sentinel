You are acting as a 15+ year expert enterprise React/TypeScript engineer, GRC platform architect, and enterprise product designer.

Your task is to build a fully functional, production-grade Access Control module for the Sentinel AI GRC Platform — "The Trust Layer for Production AI."

This module is the internal RBAC (Role-Based Access Control) system that governs which users can access which modules, pages, and actions within the platform. It is used by platform administrators, security leads, and IT governance teams at Fortune 500 organizations.

Do not generate prototypes or pseudocode.
Do not stop at architecture.
Generate complete, production-ready, compile-safe React/TypeScript code using shadcn/ui, TanStack Table, React Hook Form, and Zod.

================================================================
STACK
================================================================

- React 18
- TypeScript strict (zero `any`)
- Tailwind CSS
- shadcn/ui
- React Router v6
- TanStack Table v8
- React Hook Form
- Zod
- Sonner (toasts)
- Phosphor Icons
- date-fns

================================================================
DESIGN SYSTEM
================================================================

- Base color: Zinc
- Accent: Emerald
- Border radius: none (sharp enterprise)
- Font: Outfit or Inter
- Component library: shadcn/ui
- Table library: TanStack Table
- Form library: React Hook Form + Zod

Enforce:
- consistent page headers
- consistent card layouts
- consistent table patterns
- consistent modal/drawer patterns
- consistent toast feedback
- keyboard accessible interactions
- dark mode support via CSS variables
- zero hardcoded white backgrounds

================================================================
DATA MODEL
================================================================

Define these TypeScript interfaces strictly:

1. Department
   - id: string
   - name: string
   - code: string
   - description: string
   - headCount: number
   - createdAt: Date
   - updatedAt: Date
   - status: 'active' | 'inactive'

2. Permission
   - id: string
   - key: string            // e.g. "models:read", "risks:write"
   - label: string          // e.g. "View Models"
   - module: ModuleName     // e.g. "Model Inventory"
   - section: SectionName   // e.g. "AI Inventory"
   - action: 'read' | 'write' | 'delete' | 'export' | 'admin'
   - description: string

3. Role
   - id: string
   - name: string
   - code: string
   - description: string
   - permissions: string[]  // array of Permission.id
   - isSystem: boolean      // system roles cannot be deleted
   - color: string          // for badge display
   - userCount: number
   - createdAt: Date
   - updatedAt: Date

4. User
   - id: string
   - firstName: string
   - lastName: string
   - email: string
   - avatar?: string
   - departmentId: string
   - roleIds: string[]       // multiple roles
   - status: 'active' | 'inactive' | 'pending' | 'suspended'
   - lastLogin?: Date
   - createdAt: Date
   - updatedAt: Date

Derived from role assignment:
   - effectivePermissions: string[]  // merged from all assigned roles

================================================================
PERMISSION TREE
================================================================

Define the permission tree covering all Sherpa platform modules.

Group permissions by section and module as follows:

SECTION: Governance & Compliance
  MODULE: Policies
    - policies:read
    - policies:write
    - policies:delete
    - policies:export
  MODULE: Frameworks
    - frameworks:read
    - frameworks:write
    - frameworks:admin
  MODULE: Controls
    - controls:read
    - controls:write
    - controls:delete
  MODULE: Evidence Hub
    - evidence:read
    - evidence:write
    - evidence:delete
    - evidence:export
  MODULE: Audit Trail
    - audit_trail:read
    - audit_trail:export
  MODULE: DSR / Rights Management
    - dsr:read
    - dsr:write
    - dsr:admin
  MODULE: Consent Management
    - consent:read
    - consent:write
    - consent:admin

SECTION: Risk & Trust
  MODULE: Risk Register
    - risks:read
    - risks:write
    - risks:delete
    - risks:export
  MODULE: Financial Risk Quantification
    - financial_risk:read
    - financial_risk:write
    - financial_risk:export
  MODULE: Red Team Findings
    - red_team:read
    - red_team:write
    - red_team:admin
  MODULE: Trust Engine
    - trust_engine:read
    - trust_engine:admin
  MODULE: Guardrails
    - guardrails:read
    - guardrails:write
    - guardrails:admin
  MODULE: Incidents
    - incidents:read
    - incidents:write
    - incidents:delete
  MODULE: System Audit Log
    - system_audit:read
    - system_audit:export

SECTION: AI Inventory & Operations
  MODULE: Model Inventory
    - models:read
    - models:write
    - models:delete
    - models:export
  MODULE: Agent Registry
    - agents:read
    - agents:write
    - agents:admin
  MODULE: Agent IAM
    - agent_iam:read
    - agent_iam:write
    - agent_iam:admin
  MODULE: Multi-Agent Choreography
    - choreography:read
    - choreography:admin
  MODULE: Kill Switch Events
    - killswitch:read
    - killswitch:execute
    - killswitch:admin
  MODULE: Data Lineage
    - lineage:read
    - lineage:admin
  MODULE: Integrations
    - integrations:read
    - integrations:write
    - integrations:admin

SECTION: AI Supply Chain
  MODULE: AIBOM Registry
    - aibom:read
    - aibom:write
    - aibom:admin
  MODULE: Provenance Graph
    - provenance:read
    - provenance:admin
  MODULE: Vendor Upload Portal
    - vendor_upload:read
    - vendor_upload:write
  MODULE: Supply Chain Attestations
    - attestations:read
    - attestations:write
    - attestations:admin

SECTION: Sustainability & ESG
  MODULE: Carbon Ledger
    - carbon:read
    - carbon:write
    - carbon:export
  MODULE: Energy Efficiency
    - energy:read
    - energy:admin
  MODULE: ESG Reports
    - esg_reports:read
    - esg_reports:write
    - esg_reports:export
  MODULE: Model Efficiency Benchmarking
    - benchmarks:read
    - benchmarks:admin

SECTION: Administration
  MODULE: Access Control (this module)
    - access_control:read
    - access_control:write
    - access_control:admin
  MODULE: Settings
    - settings:read
    - settings:write
    - settings:admin
  MODULE: Notifications
    - notifications:read
    - notifications:admin

================================================================
SYSTEM ROLES (SEED DATA)
================================================================

Create these built-in roles (isSystem: true, cannot be deleted):

1. Super Admin
   - All permissions

2. CISO
   - All read + export permissions
   - risks:write, incidents:write, red_team:admin
   - trust_engine:admin, guardrails:admin
   - access_control:read

3. Compliance Officer
   - policies:*, frameworks:*, controls:*, evidence:*, audit_trail:*, dsr:*, consent:*
   - risks:read, incidents:read
   - esg_reports:*

4. AI Governance Lead
   - models:*, agents:*, agent_iam:*, choreography:*, killswitch:read
   - lineage:*, aibom:*, provenance:*
   - risks:read, trust_engine:read, guardrails:read

5. Security Engineer
   - red_team:*, incidents:*, guardrails:write
   - risks:read, agents:read, system_audit:*
   - killswitch:execute

6. ML Engineer
   - models:*, agents:read, lineage:read
   - trust_engine:read, benchmarks:*

7. Auditor (Read Only)
   - All :read and :export permissions only

8. Department Head
   - Read access to their department's assigned modules
   - incidents:read, risks:read

================================================================
SEED DEPARTMENTS
================================================================

Create 8 departments:
1. AI Platform Engineering
2. Information Security
3. Compliance & Legal
4. AI Governance & Ethics
5. Model Risk Management
6. Data Engineering
7. Internal Audit
8. Executive Office

================================================================
SEED USERS
================================================================

Create 12 realistic enterprise users spread across departments with varied role assignments, statuses, and last login dates.

Include:
- 2 Super Admins
- 2 CISOs
- 3 Compliance Officers
- 2 AI Governance Leads
- 1 Security Engineer
- 1 ML Engineer
- 1 Auditor

Ensure:
- mix of active, pending, suspended statuses
- realistic Fortune 500 enterprise names
- corporate email addresses
- varied last login dates in 2026

================================================================
ROUTES & FILE STRUCTURE
================================================================

Implement these routes and files:

Routes:
- /access-control                     → Access Control Overview / Dashboard
- /access-control/users               → Users list
- /access-control/users/:id           → User detail page (or detail sheet)
- /access-control/roles               → Roles list
- /access-control/roles/:id           → Role detail + permission editor
- /access-control/departments         → Departments list
- /access-control/departments/:id     → Department detail

File structure:
src/pages/access-control/
  index.tsx                     ← overview dashboard
  users/
    index.tsx                   ← users table
    [id].tsx                    ← user detail
  roles/
    index.tsx                   ← roles table
    [id].tsx                    ← role detail + permission editor
  departments/
    index.tsx                   ← departments table
    [id].tsx                    ← department detail

src/features/access-control/
  types.ts                      ← all TypeScript interfaces
  constants/
    permissions.ts              ← full permission tree
    system-roles.ts             ← default system role definitions
  data/
    seed.ts                     ← realistic seed data
  hooks/
    useUsers.ts
    useRoles.ts
    useDepartments.ts
    usePermissions.ts
  components/
    PermissionTree.tsx
    PermissionMatrix.tsx
    RoleForm.tsx
    UserForm.tsx
    DepartmentForm.tsx
    RoleBadge.tsx
    StatusBadge.tsx
    UserAvatar.tsx
    PermissionBadge.tsx

================================================================
MODULE 1: ACCESS CONTROL OVERVIEW
Route: /access-control
================================================================

Build a dashboard overview page with:

Metric tiles (top row):
- Total Users (with active count)
- Total Roles (with system vs custom split)
- Total Departments
- Pending Users (users awaiting activation)

Below metrics:
- Quick navigation cards to Users, Roles, Departments
- Recent user activity (last 5 user changes)
- Permission coverage summary chart (Recharts bar: which modules have most restricted access)
- System role summary table

================================================================
MODULE 2: DEPARTMENTS
Route: /access-control/departments
================================================================

Table columns:
- Department name
- Code
- Description
- Head Count
- User Count (users assigned to this department)
- Status (active/inactive)
- Created At
- Actions (edit, deactivate, delete)

Features:
- search by name or code
- filter by status
- sort by name, headcount, user count
- pagination
- bulk actions: activate, deactivate, delete selected
- click row to open department detail drawer

Department detail drawer/page:
- header: name, code, status badge
- description
- list of users in this department (with roles)
- edit button

CREATE / EDIT DEPARTMENT FORM:
Fields:
- name (required, unique)
- code (required, uppercase auto-format, unique)
- description (optional)
- status (active/inactive)

Validation (Zod):
- name: min 2, max 80 characters
- code: 2-10 uppercase alphanumeric
- description: max 300 characters

Success toast: "Department created." or "Department updated."
Delete confirmation: dialog with warning if users are assigned.

================================================================
MODULE 3: ROLES
Route: /access-control/roles
================================================================

Table columns:
- Role name (with colored badge)
- Code
- Description
- Permissions count
- User Count
- System role indicator
- Created At
- Actions (edit, duplicate, delete)

Features:
- search by name or code
- filter by system/custom
- sort by name, user count, permissions count
- pagination
- click row to open role detail
- bulk delete for custom roles only
- "Create Role" button

Role detail page (/access-control/roles/:id):
- header: role name, badge, system/custom indicator
- description
- user count with link to filtered users view
- full permission editor (see below)
- edit metadata button
- delete button (disabled for system roles with tooltip)

PERMISSION EDITOR (core component):
This is the most important UI in the entire module.

Build a grouped, hierarchical permission editor as follows:

Layout:
- Left: Section accordion (Governance, Risk, AI Inventory, Supply Chain, ESG, Administration)
- Right: Permission checkboxes grouped by module within each section
- Top: "Select all" toggle per section
- Each module has a "Module select all" toggle
- Individual permission checkboxes per action (read, write, delete, export, admin)
- Visual indicator when partial vs full module access is granted

Permission matrix table alternative view:
- Rows: modules
- Columns: read | write | delete | export | admin
- Checkboxes at each intersection
- Row header: section group + module name
- Compact, scannable, enterprise table aesthetic
- Sync between accordion and matrix views via shared state

Features:
- live count of selected permissions
- "Select All Permissions" master toggle
- unsaved changes warning before navigation
- save with confirmation toast

CREATE ROLE FORM:
Fields:
- name (required, unique)
- code (required, unique, auto-suggested from name)
- description (optional)
- color (pick from preset enterprise palette: zinc, emerald, blue, amber, red, purple, orange)
- permissions (required, use permission tree editor)

Validation (Zod):
- name: min 2, max 80
- code: unique, auto-format
- permissions: at least 1 required

Duplicate role:
- pre-fills form with "Copy of [name]" and same permissions
- useful for creating variants of system roles

EDIT ROLE:
- same form, pre-populated
- shows current user count
- shows diff of changed permissions
- system roles: allow editing description only, not permissions

================================================================
MODULE 4: USERS
Route: /access-control/users
================================================================

Table columns:
- Avatar + Full Name
- Email
- Department (badge)
- Roles (max 2 visible badges, "+ N more")
- Status badge (active, inactive, pending, suspended)
- Last Login (relative time with tooltip absolute)
- Created At
- Actions: view, edit, suspend, reset password, delete

Features:
- search by name or email
- filter by: department, role, status
- active filter count
- clear all filters
- sort by name, last login, created date, department
- pagination (20 per page)
- bulk actions: activate, suspend, delete, assign role, change department
- export filtered results to CSV
- click row → detail drawer

User detail drawer:
- header: avatar, full name, email, status badge
- Meta: department, join date, last login
- Assigned roles (as cards with permission counts)
- Effective permissions view (merged from all roles, grouped by section)
- Activity log (recent actions by/to this user)
- Edit button
- Suspend/Activate button
- Reset password button

CREATE USER FORM (multi-step, 3 steps):
Step 1 — Identity:
- First name (required)
- Last name (required)
- Email (required, unique, valid email)
- Status (default: pending)

Step 2 — Organization:
- Department (required, dropdown from departments list)
- Notes (optional)

Step 3 — Access:
- Roles (required, multi-select with search)
- Shows: combined effective permissions preview as you select roles
- Warning: show if combined roles grant admin-level access

Validation (Zod per step):
- email: unique, valid format
- at least one role required
- department required

Form UX:
- step indicator at top
- "Back" and "Continue" navigation
- step 3 shows live effective permissions as roles are toggled
- final step shows summary before submit

Success: "User created. An activation email will be sent." toast
Error: inline validation + error toast

EDIT USER FORM:
- same structure
- shows current state
- highlights changed fields
- role change diff preview

SUSPEND USER:
- confirmation dialog
- reason field (optional)
- shows impact: "This will revoke access to N modules."

DELETE USER:
- confirmation dialog with name input
- disabled for own account

================================================================
SHARED COMPONENTS
================================================================

Build these reusable components:

1. PermissionTree.tsx
   - hierarchical tree view of all permissions
   - checkboxes with indeterminate state for partial selection
   - section expand/collapse
   - module expand/collapse
   - "select all" per level
   - visual indicators for fully granted / partially granted / not granted

2. PermissionMatrix.tsx
   - table format of permission tree
   - rows = modules, columns = actions
   - checkbox intersections
   - color-coded: granted (emerald), denied (zinc), N/A (dash)
   - compact mode for display-only (user detail)
   - edit mode (role editor)

3. RoleForm.tsx
   - reusable for both create and edit
   - embeds PermissionTree or PermissionMatrix
   - Zod-validated
   - dirty state tracking

4. UserForm.tsx
   - multi-step form
   - step indicator
   - department + role selectors
   - effective permissions preview panel

5. DepartmentForm.tsx
   - simple single-step form
   - Zod-validated

6. RoleBadge.tsx
   - colored badge by role.color
   - tooltip with role description and permission count

7. StatusBadge.tsx
   - active: emerald
   - inactive: zinc
   - pending: amber
   - suspended: red

8. PermissionBadge.tsx
   - compact badge for module:action pairs
   - used in user detail view

9. UserAvatar.tsx
   - initials fallback
   - consistent sizing

10. AccessControlLayout.tsx
    - shared layout for the module
    - secondary tab nav: Overview / Users / Roles / Departments
    - permission gate: only access_control:read can view, access_control:write can create/edit

================================================================
PERMISSION GATING (FRONTEND)
================================================================

Implement a usePermissions hook:

usePermissions():
  - returns: { can: (permission: string) => boolean, hasRole: (role: string) => boolean }
  - reads from the current user's effectivePermissions
  - supports checks like: can('models:write'), hasRole('CISO')

Implement a PermissionGate component:
  - props: permission, role, fallback
  - conditionally renders children or fallback
  - used throughout the app to show/hide actions based on user permissions

In Access Control module:
  - "Create Role" button only visible if access_control:write
  - "Delete Role" only visible if access_control:admin
  - Permission editor in role form: disabled if read-only
  - User form "Create User" gated on access_control:write
  - All destructive actions gated on access_control:admin

================================================================
STATE MANAGEMENT
================================================================

Use local state + hooks for now.
Simulate async operations with 300–500ms delay for realistic loading.
Implement:
  - useUsers hook with: users, loading, error, create, update, delete, suspend
  - useRoles hook with: roles, loading, error, create, update, delete, duplicate
  - useDepartments hook with: departments, loading, error, create, update, delete

All mutations must:
  - show loading state on submit button
  - show success toast via Sonner
  - show error toast if operation fails
  - update local state optimistically

================================================================
FORM VALIDATION (ZOD SCHEMAS)
================================================================

DepartmentSchema:
  - name: z.string().min(2).max(80)
  - code: z.string().min(2).max(10).regex(/^[A-Z0-9_]+$/)
  - description: z.string().max(300).optional()
  - status: z.enum(['active', 'inactive'])

RoleSchema:
  - name: z.string().min(2).max(80)
  - code: z.string().min(2).max(30)
  - description: z.string().max(300).optional()
  - color: z.enum(['zinc', 'emerald', 'blue', 'amber', 'red', 'purple', 'orange'])
  - permissions: z.array(z.string()).min(1, "Select at least one permission")

UserCreateSchema:
  - firstName: z.string().min(1).max(50)
  - lastName: z.string().min(1).max(50)
  - email: z.string().email()
  - departmentId: z.string().min(1)
  - roleIds: z.array(z.string()).min(1, "Assign at least one role")
  - status: z.enum(['active', 'pending'])

UserEditSchema: same as UserCreateSchema but all fields optional except email.

================================================================
EMPTY STATES
================================================================

Design proper empty states for each table:

No departments:
  "No departments yet.
  Create your first department to organize users and access structures."
  CTA: + Create Department

No roles:
  "No custom roles created.
  System roles are always available. Create a custom role to define tailored access."
  CTA: + Create Role

No users:
  "No users found.
  Invite your first user to the platform."
  CTA: + Invite User

No filtered results:
  "No results match your current filters.
  Try adjusting your search or clearing the filters."
  CTA: Clear Filters

================================================================
AUDIT TRAIL INTEGRATION
================================================================

Every CRUD operation in Access Control should generate a structured audit event:

Event shape:
  - timestamp
  - actor (current user id + name)
  - action: 'user.created' | 'user.updated' | 'user.suspended' | 'role.created' | 'role.updated' | 'department.created' etc.
  - resourceType: 'User' | 'Role' | 'Department'
  - resourceId
  - resourceName
  - before: partial snapshot (for updates)
  - after: partial snapshot (for updates)
  - ip: "127.0.0.1" (simulated)

Show these events in:
  - User detail activity log
  - Role detail activity log
  - Department detail activity log
  - Feeds into /system/audit-log

================================================================
OUTPUT FORMAT
================================================================

Work in this order:

1. Types and constants
   - types.ts (all interfaces)
   - permissions.ts (full permission tree as typed array)
   - seed.ts (departments, roles, users)

2. Hooks
   - useUsers.ts
   - useRoles.ts
   - useDepartments.ts
   - usePermissions.ts

3. Shared components
   - PermissionTree.tsx
   - PermissionMatrix.tsx
   - RoleBadge.tsx
   - StatusBadge.tsx
   - UserAvatar.tsx

4. Department module
   - departments/index.tsx
   - DepartmentForm.tsx

5. Roles module
   - roles/index.tsx
   - roles/[id].tsx
   - RoleForm.tsx

6. Users module
   - users/index.tsx
   - users/[id].tsx
   - UserForm.tsx

7. Overview dashboard
   - access-control/index.tsx

8. Route wiring
   - Add all routes to app router
   - Add Access Control section to sidebar
   - Wire sidebar badge with pending users count

After architecture section, generate actual code files.
Output complete files, not fragments.
Keep naming consistent across files.
Do not leave any file as "implementation left to reader."

================================================================
QUALITY BAR
================================================================

The result must:
- compile without TypeScript errors
- have zero `any` types
- have zero dead buttons or non-functional interactions
- feel enterprise-grade in every interaction
- support every CRUD operation fully
- show correct loading, empty, error, and success states
- work in dark mode
- be keyboard accessible
- be consistent with the rest of the Sherpa AI GRC Platform design system

Start now. Begin with types and constants, then proceed to hooks, then components, then pages.
