You are a 15+ year expert AI/ML, GRC, and React/TypeScript engineer.
Fix all P0/P1 bugs and implement full CRUD + detail views for 28 modules.
Most code exists — wire it up, fix broken handlers, add missing forms.

REPO: Read ALL existing files first. Fix what exists. Complete files only.
"CHECKPOINT: [file]" if context runs long.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
P0: CRITICAL FIXES (do first)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

FIX 1 — Delete confirmation everywhere:
  Find ALL trash/delete handlers across every page.
  Wrap each in ConfirmDialog before executing:
    title: "Delete {entityName}?"
    description: "This action cannot be undone."
    impactList: what gets affected (linked records)
    isDestructive: true | confirmLabel: "Delete"
  No record deleted without explicit confirmation.

FIX 2 — Merge duplicate search bars:
  Remove the full-width "Quick navigate ⌘K" bar below TopBar.
  Keep only TopBar search pill → opens CommandPalette.
  CommandPalette (Cmd+K): search + navigate + actions unified.

FIX 3 — Standardize ALL CRUD modals:
  Every modal must have: X close button (top-right) + Cancel + Primary action
  Escape key closes modal | Click backdrop closes modal
  Fix Register Model: add Cancel button
  Audit every other modal — add Cancel wherever missing

FIX 4 — Skeleton loaders (3-4s blank page fix):
  Every page: show skeleton on mount, replace with data
  Skeleton: animate-pulse bars matching layout
  Use existing Skeleton component or create:
    <div className="animate-pulse bg-[hsl(var(--border))] h-4 w-3/4"/>

FIX 5 — Toast notifications after CRUD:
  Every create → toast.success("{Entity} created")
  Every update → toast.success("{Entity} updated")
  Every delete → toast.success("{Entity} deleted")
  Every error → toast.error("Failed: {message}")
  Use sonner (already installed)

FIX 6 — Pagination on all list views:
  DataTable component: add pagination (already built — apply everywhere)
  Default pageSize: 25 | Options: 10/25/50/100
  "Showing X–Y of Z" label
  Previous / Next buttons

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
P1: MISSING CRUD BUTTONS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Compliance Calendar — add "+ Schedule Event":
  Form: Title* | Type (Audit/Review/Deadline/Training) |
    Date* | Framework | Owner | Recurrence | Description
  Events: view | edit | delete with confirm

Training & Awareness — add "+ Create Course":
  Form: Title* | Category* | Format (Interactive/Video/Document) |
    Duration (minutes)* | Target Audience (role multi-select) |
    Assigned To (user multi-select) | Due Date* | Passing Score |
    Certification toggle | Renewal Period | Content URL or upload
  Table: CRUD + completion % per course

Kill Switch "KILL ALL AGENTS" — 2-step:
  Step 1: Alert dialog "This will suspend ALL 12 agents immediately"
  Step 2: Type "KILL ALL" to confirm + Reason textarea (required)
  On confirm: all agents → quarantined + audit entry

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
MODULE CRUD SPECS (28 modules)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Each module below: implement full Create + Detail Drawer + Edit + Delete.
All detail drawers: 640px right, 3-5 tabs minimum.
All creates: Modal lg/xl with react-hook-form + zod validation.

── BUSINESS CONTINUITY (/continuity) ──
Create "+ Create BCP Plan" Button.
Form:
  Name* | Type (BCP/DR/Crisis/IncidentResponse) | Scope* |
  Owner* | Department | Priority (Critical/High/Medium/Low) |
  RTO* (hours) | RPO* (hours) | Status |
  Last Tested | Next Test Date* | Test Frequency |
  Linked AI Systems (multi-select MDL-XXX) |
  Recovery Location | Insurance Reference

Detail Drawer (5 tabs):
  Overview: all fields read-only grid
  Recovery Steps: add/remove rows (step# + description + owner + est.time)
  Dependencies: add/remove rows (system + dependency type + criticality)
  Test History: table (date + result + tested by + duration + findings)
  Contacts: escalation contacts (name + role + phone + email)

Table columns: Plan | Type | Owner | RTO | RPO | Last Tested |
  Next Test | Status | Actions (Eye + Edit + Delete)

── BENCHMARKING & MATURITY (/maturity) ──
AI Governance Maturity Model (Level 1-5):
Create "+ Start Assessment" Button.
Form:
  Assessment Name* | Framework (ISO 42001/NIST/Custom) |
  Assessor* | Scope (org-wide/department/use-case) |
  Assessment Date* | Target Maturity Level

Assessment wizard (5 questions per domain):
  Domains: Governance | Risk | Compliance | Technical | Operations
  Per question: 1-5 rating + evidence textarea + notes

Detail Drawer (4 tabs):
  Scores: radar chart per domain + overall level
  Gap Analysis: per domain gap vs target level
  Roadmap: auto-generated improvement steps per gap
  History: previous assessments + trend

Table: Assessment | Framework | Level (1-5) | Score | Assessor |
  Date | Status | Actions

Maturity levels:
  1=Initial | 2=Developing | 3=Defined | 4=Managed | 5=Optimizing
  Badge colored: 1=error 2=warn 3=info 4=ok 5=brand

── TRAINING & AWARENESS (/training) ──
(Fix missing "+ Create Course" — P1 above)
Detail Drawer (4 tabs):
  Overview: course metadata
  Participants: per-user completion status + last attempted + score
    Status: Not Started | In Progress | Completed | Failed | Overdue
    "Send Reminder" Button per overdue participant
  Content: description + materials + assessment questions
  Activity: enrollment + completion events log

Dashboard metrics: Completion Rate % | Overdue Assignments |
  Certified Users | Average Score

── STAKEHOLDER & COMMITTEE MANAGEMENT (/committee) ──
Create "+ Create Committee" Button.
Form:
  Committee Name* | Type (AI Ethics/Risk/Audit/Advisory) |
  Chair* (user select) | Members (multi-user select) |
  Meeting Frequency (Monthly/Quarterly/Ad-hoc) |
  Mandate* (textarea) | Status (Active/Inactive)

Detail Drawer (4 tabs):
  Overview: metadata grid
  Members: member list + role + join date + "Remove" per row
  Meetings: upcoming + past meetings
    Per meeting: date + agenda + minutes + attendees + decisions
    "+ Log Meeting" → form with agenda + minutes upload
  Decisions: all committee decisions with linked entities

Also: Stakeholder Register
Create "+ Register Stakeholder" Button.
Form:
  Name* | Role* | Organization | Type (Internal/External) |
  Interest Level (1-5) | Influence Level (1-5) |
  Communication Preference | Contact Info |
  Linked Use Cases (multi-select) | Notes

── MODEL EFFICIENCY BENCHMARKING (/model-efficiency) ──
Pareto frontier chart (Recharts ScatterChart):
  X-axis: Cost per 1K tokens ($) | Y-axis: Quality Score (0-100)
  Each point: one model, colored by risk tier
  Hover: model name + all metrics
  "Pareto frontier" line connecting optimal models

Table: Model | Quality | Cost | Latency | Carbon | Efficiency Score | Rank

Create "+ Add Benchmark Run" Button.
Form:
  Model* | Benchmark Suite* | Dataset* | Date* |
  Quality Score | Latency (ms) | Cost per 1K ($) |
  Carbon (gCO₂) | Notes

Detail Drawer (3 tabs):
  Results: all metrics with vs-baseline comparison
  Recommendations: quantization | distillation | caching opportunities
  History: previous runs trend chart

ELO-style ranking: calculate from head-to-head wins

── ESG REPORTS (/esg-reports) ──
Create "+ Generate ESG Report" Button.
Form:
  Report Name* | Period (Q1/Q2/Q3/Q4/Annual)* |
  Year* | Framework (GRI/SASB/TCFD/Custom) |
  Author* | Status (Draft/Review/Published) |
  Scope (AI Systems / Full Org)

Report sections auto-populated from data:
  Governance: policy count + framework scores + audit findings
  Risk: open risk count + critical incidents + MTTR
  Fairness: bias audit results + protected attribute coverage
  Transparency: model card completeness + explainability coverage
  Carbon: total emissions + vs target + efficiency score

Detail Drawer (4 tabs):
  Overview: report metadata + status
  Content: section-by-section preview with edit
  Sign-off: approval chain (author → DPO → Board)
  Export: PDF download + regulatory submission tracking

── ENERGY EFFICIENCY (/energy-efficiency) ──
Per-model energy efficiency score:
  Formula: Quality Score ÷ (GPU hours × carbon_factor) × 100

Table: Model | GPU Hours/Week | Quality Score | Efficiency Score |
  Recommendation | Actions

Create "+ Log Energy Reading" Button.
Form: Model* | GPU Hours* | Period* | Measurement Source | Notes

Detail Drawer (3 tabs):
  Metrics: efficiency trend chart | GPU utilization % | kWh consumed
  Recommendations: quantize (save N%) | prune | cache | smaller model
  Comparison: efficiency vs peer models benchmark

Recommendations panel:
  Each: model name + action + estimated saving % + effort (Easy/Medium/Hard)
  "Implement" → creates task auto-linked to model

── CARBON LEDGER (/carbon-ledger) ──
Enhance existing (already has log entry — improve detail):
Create "+ Log Entry" Button (fix if broken).
Form:
  Source* (Model/Agent/Infrastructure/Other) |
  Linked Model (MDL-XXX select) | Period* |
  Emissions (kgCO₂)* | Measurement Method |
  Scope (1/2/3) | Notes

Detail Drawer (3 tabs):
  Breakdown: per-model emissions donut chart
  Trend: 12-month line chart vs carbon budget
  Recommendations: AI-generated reduction suggestions per model

Budget enforcement:
  "Set Budget" → number input (tCO₂/month)
  If exceeded: error banner "111% over budget" + recommendations

── CONSENT MANAGEMENT (/consent-management) ──
Create "+ Register Consent Record" Button.
Form:
  Data Subject Reference (anonymized ID)* |
  Consent Type (Marketing/Processing/Research/Automated-Decision) |
  Legal Basis (Art.6a/6b/6c/6e/6f)* |
  Purpose* | Collection Date* | Expiry Date |
  Collection Channel | Linked Dataset (DS-XXX) |
  Consent Text (what was agreed to) | Withdrawal Method

Detail Drawer (4 tabs):
  Overview: all fields grid
  Consent History: changes to consent over time (granted/withdrawn/renewed)
  Linked Data: which datasets process this subject's data
  Withdrawal: "Process Withdrawal" Button → marks withdrawn + triggers erasure task

Table: ID | Type | Legal Basis | Purpose | Collected | Expires |
  Status (Active/Withdrawn/Expired) | Actions

Consent purpose registry (second tab on main page):
  Table of purposes: name + legal basis + retention period + active consents count

── DSR / RIGHTS MANAGEMENT (/dsr) ──
Enhance existing (already has CRUD — improve):
Detail Drawer (5 tabs):
  Overview: request metadata + GDPR article + SLA countdown
  Processing Steps: checklist
    ☑ Identity verified | ☑ Systems searched | ☐ Data compiled |
    ☐ Response sent | ☐ Logged in register
  Linked Records: affected models + datasets + agents
  Response: generated response text + "Send Response" Button
  Audit: all actions taken on this request

SLA enforcement:
  GDPR 30-day: countdown timer on each request
  <7 days: error badge "URGENT" | 7-14d: warn | >14d: ok
  Overdue: "SLA BREACHED" banner + escalate to DPO

Right to Explanation flow:
  If type = Explanation: "Generate Explanation" Button
  Links to Explainability Center → per-decision SHAP output
  "Generate Letter" → template with plain-language explanation

── INTEGRATIONS (/integrations) ──
Enhance existing (has Add Integration — improve):
Detail Drawer (4 tabs):
  Overview: connection metadata + health status
  Configuration: credential fields (masked) + endpoint + options
  Sync History: last N syncs with status + records synced + errors
  Webhook Events: which events trigger this integration

"Test Connection" Button → 2s simulated → Pass/Fail toast

Integration categories with real-form fields per type:
  Jira: Server URL + Project Key + API Token + Issue Type mapping
  Slack: Webhook URL + Channel + Notification types (multi-select)
  Splunk: HEC URL + Token + Index + Source type
  AWS: Access Key + Secret + Region + Service (SageMaker/S3/CloudWatch)
  ServiceNow: Instance URL + Username + Password + Table mapping
  PagerDuty: API Key + Service ID + Severity mapping

Status per integration: Connected (ok) | Error (error) | Syncing (pulse)

── APPROVAL WORKFLOWS (/workflows) ──
Enhance existing (has New Workflow):
Visual workflow builder (click workflow → /workflows/:id):
  SVG flow diagram:
    Trigger node (blue) → Stage cards → Decision nodes → End node (green)
  Stage card: name + approver role + SLA hours + escalation
  Click stage → right config panel:
    Stage name | Approver (role/user) | SLA (hours) |
    Escalation: after N hours → assign to | Notify via (Slack/Email)
    Condition: IF risk_level = critical (add/remove conditions)
  Between stages: condition badge on arrow
  Add stage: "+ Add Stage" button after last stage

Workflow types:
  Sequential | Parallel | Conditional (routing based on field value)

Active workflow instances table (second tab):
  Workflow | Entity | Current Stage | Assignee | Due | Days Left | Actions

── VENDOR REGISTRY (/vendors) ──
Enhance existing (already has Add Vendor):
Add missing fields to vendor form:
  Contract Expiry Date | Annual Spend ($) | SLA Terms |
  Contract Reference | Renewal Notice (days) | Auto-renewal toggle |
  Primary Contact (name + email + phone) |
  Security Contact | DPO Contact |
  Sub-processors (add/remove rows: company + location + purpose) |
  Data Processing Countries (multi-select) |
  GDPR Transfer Mechanism (SCCs/BCRs/Adequacy/N/A) |
  Cyber Liability ($) | Exit Plan (textarea)

Detail Drawer (6 tabs):
  Overview: metadata grid
  Scorecard: 4 sliders read-only (Data Sensitivity / Criticality /
    Incidents / Regulatory) → live tier calculation
  Contract: expiry date + days-remaining badge + value + SLA terms
    Days to expiry: <30d error | <60d warn | >60d ok
  Documents: upload/link DPA | SOC2 | ISO Cert | Pen Test | Insurance
    Per doc: name + type + uploaded + expiry + download + delete
  Performance: SLA violations count + incident count + overall score
  Actions: Enable/Disable toggle | "Request DPA" | "Schedule Review"

Enable/Disable: ToggleSwitch → ConfirmDialog:
  Disable: "Disabling V-001 will flag all linked models for review"
  Enable: immediate with toast

Cost Center allocation: input field on vendor form
EOL (End of Life) date: date field + warning badge if approaching

── DATA LINEAGE (/data-lineage) ──
Create "+ Register Lineage Record" Button.
Form:
  Name* | Source Dataset (DS-XXX)* |
  Transformation Steps (add/remove rows: step + tool + owner) |
  Output Dataset or Model (DS/MDL) |
  Version | Processing Date | Owner*

Visual DAG (main view):
  Nodes: DS chips (blue) → Transform boxes (amber) → Model circles (green)
  Edges: arrows with transformation label
  Click node: popover with metadata + navigate EntityChip
  "Impact Analysis": select a node → highlight all downstream dependencies
  "What breaks if DS-001 changes?" → highlight affected models

Detail Drawer (3 tabs):
  Overview: lineage metadata
  Upstream: ancestor datasets + sources
  Downstream: all models + outputs that depend on this dataset
    Impact count: "Changing this affects 3 models + 2 use cases"

── DATASET REGISTRY (/datasets) ──
Enhance existing (has Add Dataset):
Improve form with missing fields:
  Data Source URL | Collection Method |
  GDPR Legal Basis (Art.6 select)* | Consent Record ID |
  Retention Policy (30d/90d/1y/3y/7y/Forever) |
  Data Subject Rights handling (textarea) |
  Schema Column Count | Quality Score (slider 0-100) |
  Data Sharing Agreements (add/remove: partner + purpose + expiry)

Detail Drawer (5 tabs):
  Overview: metadata grid
  Schema: column list (name + type + PII flag + description)
    "+ Add Column" row
  Linked Models: EntityChip list of models trained on this dataset
  Compliance: GDPR Art.5 checklist (purpose limitation, minimization, etc.)
  Audit History: re-audit events + results

── SUPPLY CHAIN ATTESTATIONS (/supply-chain) ──
Create "+ Add Attestation" Button.
Form:
  Title* | Vendor (V-XXX)* | Attestation Type
    (SOC2/ISO27001/PenTest/ModelCard/DataSheet/Security) |
  Issued By* | Issue Date* | Expiry Date* |
  Scope (textarea) | Digital Signature | File Upload

Detail Drawer (4 tabs):
  Overview: metadata grid
  Document: file preview iframe or download
  Verification: digital signature status + verification method
  Renewal: expiry countdown + "Request Renewal" Button

Status badges: Valid (ok) | Expiring (warn <30d) | Expired (error) | Pending

Table: ID | Title | Vendor | Type | Issued | Expires | Status | Actions

── VENDOR UPLOAD PORTAL (/vendor-upload) ──
Note: This is SEPARATE from Vendor Registry.
Vendor Upload Portal = external-facing intake for VENDORS to submit their own docs.
Vendor Registry = internal management of vendors.

Build /vendor-upload as a dedicated intake page:
  "Submit Vendor AI Disclosure" form:
    Company Name* | Contact Email* | Product Name* |
    Submission Type (Model Card/Data Sheet/Security Attestation/DPA) |
    File Upload (PDF/DOCX)* | Description |
    Frameworks claimed compliance with (checkboxes) |
    Attestation statement checkbox*

  On submit: creates pending record in Vendor Registry (status=pending_review)
  Auto-notification to assignee

  Admin review queue (/vendor-upload/review):
    Table of submitted disclosures with: accept | reject | request-info actions
    Accept → moves to Vendor Registry + creates attestation record

── AIBOM REGISTRY (/aibom) ──
AI Bill of Materials — tracks model component dependencies.
Enhance existing:

Detail Drawer (4 tabs):
  Overview: AIBOM metadata (model + version + generated date + format)
  Components: tree view
    Root: model name
      ├── Training Data: DS-XXX chips
      ├── Libraries: name + version + license + CVE count
      ├── Framework: PyTorch/TensorFlow + version
      └── Hardware: GPU type + provider
  Vulnerabilities: CVE table from component scan
    Per CVE: ID + CVSS + component + status + patch available
  Compliance: license conflict detection
    "MIT ✓ | GPL-3.0 ⚠ Copyleft restriction | Apache-2.0 ✓"

"Generate AIBOM" → creates new record from selected model
  Format: SPDX | CycloneDX | Custom
  Export: JSON download

Table: AIBOM ID | Model | Version | Components | CVEs | License Issues |
  Generated | Status | Actions

── FINANCIAL RISK QUANTIFICATION (/financial-risk) ──
Build from scratch using FAIR methodology (simplified).

Create "+ New Risk Quantification" Button.
Form:
  Name* | Linked Risk (RSK-XXX) | Asset Name* |
  Asset Value ($)* | Threat Frequency (per year) |
  Vulnerability Factor (0-1 slider) |
  Impact Factor (0-1 slider) |
  Control Effectiveness (0-100% slider) |
  Analysis Date* | Analyst*

Auto-calculation (live, updates as sliders move):
  Loss Event Frequency = Threat Frequency × Vulnerability Factor
  Loss Magnitude = Asset Value × Impact Factor × (1 - Control Effectiveness)
  ALE (Annual Loss Expectancy) = LEF × LM
  Show: "Estimated ALE: $2.4M — $8.7M (confidence range)"

Display: colored ALE badge
  >$10M: error | $1M-10M: warn | <$1M: ok

Detail Drawer (3 tabs):
  Analysis: all inputs + calculated outputs in grid
  Scenarios: Best Case | Most Likely | Worst Case columns
  Recommendations: controls that would reduce ALE + estimated savings

Table: Name | Linked Risk | Asset | ALE | LEF | Last Updated | Analyst

── EXCEPTION MANAGEMENT (/exceptions) ──
Enhance existing (has Request Exception):
Improve form:
  Exception Title* | Type (Policy/Control/Regulatory) |
  Exception Request* | Business Justification* |
  Requestor* | Risk Owner | Risk Level |
  Compensating Controls (add/remove rows: control + effectiveness %) |
  Requested Expiry* | Business Impact if Denied (textarea) |
  Risk Acceptance Statement* | Board Approval Required toggle

Detail Drawer (5 tabs):
  Overview: all fields grid
  Approval Chain: current stage + decision history
    Requestor → Risk Manager → CISO → Board (if critical)
    Each stage: approve | reject | request-info
  Compensating Controls: list with effectiveness %
  Renewal: expiry countdown + "Request Extension" Button
    Extension form: new expiry + justification + approver
  Activity: all events log

Status: Requested | Under Review | Approved | Rejected | Expired | Renewed
  Expired exceptions: red badge + auto-create renewal task

── RISK REGISTER (/risk) ──
Enhance existing (already has Add Risk — improve):
Ensure "+ Register Risk" Button opens full form:
  Title* | Category* | Description |
  Likelihood* (1-5 slider: Rare→Almost Certain) |
  Impact* (1-5 slider: Negligible→Catastrophic) |
  Risk Score: live L×I calculation with colored badge |
  Treatment* (Accept/Mitigate/Transfer/Avoid) |
  Treatment Plan (textarea) | Target Date |
  Owner* | Department |
  Linked Model (MDL-XXX) | Linked Use Case (UC-XXX) |
  Linked Controls (multi-select CTRL-IDs) |
  Residual Likelihood (1-5 slider) | Residual Impact (1-5 slider) |
  Residual Score: live calculation |
  Tags

Detail Drawer (5 tabs):
  Overview: inherent risk vs residual risk side-by-side
    Two score badges: "Inherent: 20 Critical" → "Residual: 9 Medium"
    Risk appetite indicator: "Appetite: Medium | Current: High"
  Treatment Plan: milestones table (description + owner + deadline + status)
  Linked Entities: controls + incidents + models as EntityChips
  Financial: link to /financial-risk quantification for this risk
  Activity: all changes + approvals log

5×5 heatmap (top of page):
  CSS grid | click cell → filter table to that L×I
  List/Matrix view toggle button (sessionStorage state)

ISO 31000 statuses: Open | In-Treatment | Accepted | Transferred |
  Mitigated | Archived
  "Accepted" requires: acceptance rationale + acceptor + ConfirmDialog

── DOCUMENT MANAGEMENT (/documents) ──
Enhance existing (fix "coming soon" button):
4-step creation wizard:
  Step 1 — Metadata:
    Title* | Type* (Policy/Standard/Procedure/Guideline/Record) |
    Version (default "1.0") | Owner* | Category |
    Framework Links (multi-select) | Review Date* | Status

  Step 2 — Content:
    Toggle: Upload File OR Write Content
    Upload: drag-drop zone (PDF/DOCX/MD)
    Write: Textarea sections builder
      Add section: heading + content textarea | drag to reorder

  Step 3 — Framework Mapping:
    Per linked framework: which clauses this satisfies

  Step 4 — Review: summary + Create

Detail Drawer (4 tabs):
  Overview: metadata grid
  Content: rendered section content | "Edit" mode
  Version History: v1.0 → v1.1 → v2.0 changelog
    Per version: date + author + changes + "View Snapshot"
  Activity: all edits + approvals

Edit: re-opens wizard pre-populated
Archive: soft-delete with justification (never hard-delete)

── THREAT INTELLIGENCE (/security/threats) ──
Enhance existing (already has Add Threat):
Detail Drawer (4 tabs):
  Overview: threat metadata + MITRE ATT&CK ID + tactic
  Affected Systems: linked models + agents + use cases
  Remediation: recommended actions + owner + deadline
    "Create Remediation Task" → task auto-linked
  Activity: investigation timeline events

MITRE ATLAS mapping (AI-specific threats):
  AML.T0000 variants for adversarial ML threats
  Per threat: technique ID + tactic + mitigation reference

Table: add "MITRE ID" column + "Source" column (threat feed name)
"Investigate" action: status → Investigating + HITL item created
"Resolve" action: ConfirmDialog + resolution notes required (min 100 chars)

── ATTACK SURFACE (/security/attack-surface) ──
Enhance existing:
Detail Drawer (4 tabs):
  Overview: asset metadata (type/exposure/owner/last scan)
  Risk Profile: linked risks + CVSS for this asset + exposure score
  Scan History: table (date + scan type + findings + duration)
    "Run Scan" Button → 3s simulated → updates last scan timestamp
  Linked Systems: which models/agents serve this endpoint

Visual topology (main page):
  SVG node-link diagram
  External nodes: red circle (exposed) | Internal: blue
  Edge width: proportional to data flow
  Click node: asset detail popover
  "Exposed" pulse animation on critical assets

─ PERFORMANCE MONITORING (/performance-monitoring) ──
This module tracks AI system runtime performance.
Build with:

4 MetricTiles: Avg Latency(ms) | P99 Latency | Error Rate % | Throughput/sec

Per-model performance table:
  Model | Avg Latency | P99 Latency | Error Rate | SLA Status |
  Evaluations/hr | Last Updated

SLA tracking per model:
  Configure: Latency SLA (ms) + Error Rate SLA (%)
  Breach: error badge + alert notification
  History: SLA compliance % trend chart

Alert thresholds (configure per model):
  Latency > N ms → alert | Error Rate > N% → alert
  "Configure Alerts" → modal with threshold inputs + channel select

Detail Drawer (3 tabs):
  Metrics: time-series charts (Recharts LineChart)
    Latency trend | Error rate trend | Throughput trend
  Alerts: active alerts + history
  SLA: compliance % over time + breach log

── MULTI-AGENT CHOREOGRAPHY (/multi-agent) ──
Enhance existing (has New Workflow):
"New Workflow" → 3-step wizard:
  Step 1 — Workflow basics:
    Name* | Type (Sequential/Parallel/Conditional) |
    Description | Owner* | Trigger (Manual/Scheduled/Event)

  Step 2 — Agent chain builder:
    Add agents: select AGT-XXX from list → add to chain
    Per agent in chain:
      Input mapping | Output mapping | Conditions (if conditional)
      HITL checkpoint: toggle (pause here for human review)
      Fallback: select alternative agent if this fails
    Drag to reorder agents in chain

  Step 3 — Review: chain diagram preview + Create

Chain visualization: existing (keep) + enhance:
  Click edge → message sample drawer (what data flows between agents)
  Click agent node → agent detail popover
  Token/cost tracking per hop (already exists — enhance display)

Detail Drawer (4 tabs):
  Overview: workflow metadata + chain stats
  Execution Log: run history (date + status + duration + tokens + cost)
    Per run: expand → per-agent step timing + output
  Performance: latency per hop bar chart
  Config: HITL checkpoint positions + fallback configuration

── AGENT IAM (/agent-iam) ──
Create "+ Register IAM Policy" Button.
Form:
  Policy Name* | Linked Agent (AGT-XXX)* |
  Effective Date* | Expiry Date |
  Description | Status (Active/Inactive/Draft)

Permission matrix (per policy):
  Rows: Resources (datasets, models, APIs, tools, outputs, external-services)
  Cols: Actions (Read/Write/Execute/Admin/Delete)
  Cells: Allow | Deny | Conditional
  Conditional: specify condition (e.g., "Allow only during business hours")

Detail Drawer (4 tabs):
  Overview: policy metadata
  Permissions: full matrix read-only
  Edit Permissions: toggle edit mode → matrix becomes interactive
    Change cells → Save → requires HITL approval if production agent
  Anomaly Detection: unusual permission use flagged
    "AGT-003 called /admin endpoint 47 times — not in IAM policy"
    "Create Alert Rule" per anomaly
  Activity: all permission changes log

Anomaly table (second tab on main page):
  Agent | Resource | Action | Count | Expected | Risk | Status
  "Enforce Least Privilege" per row → strips unauthorized permissions

── AI IMPACT ASSESSMENTS (/aiia) ──
Enhance existing (fix "+ New Assessment" if broken):
5-step wizard:
  Step 1 — Template: EU AI Act Art.27 | GDPR Art.35 DPIA |
    NIST RMF Assessment | Combined AIIA+DPIA

  Step 2 — AI System:
    Select Model* (MDL-XXX searchable list) |
    EU AI Act Annex III category (if high-risk) |
    Automated decisions: toggle | Affects vulnerable groups: toggle |
    Estimated affected population*

  Step 3 — Rights Impact:
    Per right checklist (Non-discrimination/Privacy/Labor/Justice/Children):
      Impact: None | Low | Medium | High | Critical
      Justification textarea per right

  Step 4 — Mitigations:
    Identified harms table (add/remove):
      Harm + Likelihood(1-5) + Severity(1-5) + Mitigation + Owner + Deadline
    Link existing RSK-XXX chips

  Step 5 — Review & Sign-off:
    Summary read-only | DPO consultation notes |
    Regulator notification toggle |
    "Submit for Approval" → creates HITL item

Detail Page (/aiia/:id) — 6 tabs:
  Overview: metadata grid
  Rights Analysis: per-right impact table + radar chart
  Risk Register: linked RSK-IDs table with navigate
  Mitigations: harm → mitigation table
  Approval Chain: Assessor → DPO → Risk Officer → CISO timeline
  Activity: all events log

"Generate AIIA Report PDF" → Blob download
"Export EU AI Act Annex IV" → structured JSON

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
SIDEBAR EXPANDABLE BUTTONS FIX
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
"Model Inventory >" "Trust Engine >" etc. are buttons not links.
Fix: Make the section header itself navigate to primary sub-route.
  "Model Inventory >" → navigate("/models/inventory") + toggleExpand()
  "Trust Engine >" → navigate("/trust-engine") + toggleExpand()
  "Agent Discovery >" → navigate("/agents") + toggleExpand()
  "Security Overview >" → navigate("/security") + toggleExpand()
  "Evidence >" → navigate("/evidence-sync") + toggleExpand()
  "Policies >" → navigate("/compliance/policies") + toggleExpand()
  "Risk Register >" → navigate("/risk") + toggleExpand()
  "Incidents >" → navigate("/risk/incidents") + toggleExpand()
  "Quality Metrics >" → navigate("/evals") + toggleExpand()

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
OUTPUT ORDER
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Global fixes first:
1.  src/components/ui/ConfirmDialog.tsx  (ensure universal — apply everywhere)
2.  src/components/ui/DataTable.tsx     (add pagination to all tables)
3.  src/components/layout/Sidebar.tsx  (fix expandable buttons → navigate)
4.  src/components/layout/TopBar.tsx   (remove duplicate search bar)

Then each module (complete file per module):
5.  src/pages/Continuity.tsx
6.  src/pages/Maturity.tsx
7.  src/pages/Training.tsx
8.  src/pages/Committee.tsx
9.  src/pages/ModelEfficiency.tsx
10. src/pages/ESGReports.tsx
11. src/pages/EnergyEfficiency.tsx
12. src/pages/CarbonLedger.tsx
13. src/pages/ConsentManagement.tsx
14. src/pages/DSR.tsx
15. src/pages/Integrations.tsx
16. src/pages/Workflows.tsx + WorkflowBuilder.tsx
17. src/pages/Vendors.tsx
18. src/pages/DataLineage.tsx
19. src/pages/Datasets.tsx
20. src/pages/SupplyChainAttestations.tsx
21. src/pages/VendorUploadPortal.tsx
22. src/pages/AIBOMRegistry.tsx
23. src/pages/FinancialRisk.tsx
24. src/pages/Exceptions.tsx
25. src/pages/Risk.tsx
26. src/pages/Documents.tsx
27. src/pages/Threats.tsx
28. src/pages/AttackSurface.tsx
29. src/pages/PerformanceMonitoring.tsx
30. src/pages/MultiAgentChoreography.tsx
31. src/pages/AgentIAM.tsx
32. src/pages/AIIA.tsx + AIIADetail.tsx

After all files:
"COMPLETE ✓
 P0: Delete confirmation on all destructive actions
 P0: Duplicate search bars merged into single ⌘K
 P0: All modals: Cancel + X + Escape standardized
 P0: Skeleton loaders on all pages (3-4s blank fixed)
 P0: Toast notifications on all CRUD operations
 P0: Pagination on all tables
 P1: Compliance Calendar + Training + Kill Switch fixed
 Sidebar: all expandable buttons → navigate + expand
 28 modules: full CRUD + detail drawers + proper forms
 Every destructive action: ConfirmDialog with impact list"